package algorithms.graph;

public class find_cycles {
	/*
	 * Mark all node white initially
	 * Enterting a node mark it gray and black when exiting
	 * Use a dfs on white node. Then while dfs a gray node is reached
	 * that means it has cycle
	 */

}
