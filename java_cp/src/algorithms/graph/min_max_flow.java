package algorithms.graph;

public class min_max_flow {
	/*
	 * It is to find the minimum cost max flow or K flow
	 * 
	 * Find the lowest cost path.
	 * Increase the flow using edmond karp.
	 * Take flow and cost +ve in direction of flow and in reverse -ve
	 * 
	 */

}
